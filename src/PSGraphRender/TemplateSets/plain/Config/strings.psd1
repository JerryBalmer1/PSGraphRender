@{
    # Every user-visible string. {count} and {generatedAt} are display-time
    # tokens and are filled in the page, not here.
    PlainNodesHeading = 'Nodes ({count})'
    PlainLinksHeading = 'Links ({count})'
    PlainProvenance   = 'Generated {generatedAt}'
}
